[pytest]
base_url = https://storied-cuchufli-1e70f1.netlify.app
testpaths = tests
addopts = --html=reports/test-report.html --self-contained-html --css=reports/dark-theme.css
markers =
    smoke: critical-path checks that confirm the app is up and core flows work
    regression: broader feature and edge-case coverage