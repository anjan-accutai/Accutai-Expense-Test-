[project]
name = "accutai-tests"
version = "0.1.0"
description = "Playwright test suite for Accutai Financial Portal"
requires-python = ">=3.9"
dependencies = [
    "playwright>=1.45",
    "pytest-bdd>=8.1.0",
    "pytest-html>=4.2.0",
    "pytest-playwright>=0.5",
]

[tool.uv]
package = false
